#!/usr/bin/make -f

export DH_VERBOSE = 1
export PYBUILD_NAME = askilit

%:
	dh $@ --buildsystem=meson --with python3

override_dh_auto_configure:
	dh_auto_configure -- --prefix=/usr

override_dh_auto_install:
	dh_auto_install --destdir=debian/askilit
